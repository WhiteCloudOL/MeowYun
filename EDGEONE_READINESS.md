# EdgeOne CDN 适配与上线检查

状态：**本地已验证的范围见本阶段验收记录；EdgeOne 上线后待验证。** 没有登录 CDN 控制台、修改真实规则、发布或部署。

## 1. 当前拓扑与代码处理

按「现有源站托管 `dist/` → EdgeOne CDN → 浏览器」准备。源站产品、Web 服务器类型及其配置权限尚未确定，不假定使用 EdgeOne Pages。

- 保留 Vue 3 / Vite 静态构建；入口和动态模块继续使用 Vite 内容哈希，没有添加时间戳查询串绕缓存。
- `build.manifest` 输出 `.vite/manifest.json`，可据此识别本次构建实际生成的版本化资源。它是发布工具的清单，不应成为访客需要请求的资源；源站应拒绝访问点目录。
- 已为当前真实存在的作品索引、6 个作品详情、关于、小记、漫游、工坊、明信片和口袋生成静态 Meta 入口。明信片、口袋不进入 Sitemap，入口标记 `noindex`。原文章、标签、短链保持。
- 开发工具插件只在开发服务启用；生产构建不带开发工具悬浮层。
- 分块加载失败会提示网络/版本问题，保留当前页面，普通页面由用户主动重新加载；明信片页不给刷新捷径，用户先返回画布导出 PNG。没有无限自动刷新，也不会清空主题或收藏。编辑草稿目前仍是本页临时状态，PNG 只保存画面，不等于可继续编辑的草稿备份。
- 当前没有 Service Worker、登录、用户接口或范围请求音频；不新增离线缓存、后端或泛用 CORS 规则。
- `scripts/verify-origin.mjs` 是**本地测试源站**，用于核验分类响应头、Brotli/gzip、404 和构建入口；不是对真实源站的配置，也不是 EdgeOne 缓存模拟器。运行 `npm run build` 后运行 `node scripts/verify-origin.mjs`，访问 `http://127.0.0.1:4183`。

## 2. 缓存规则表（待源站/控制台执行）

以下是本轮迭代基线。规则优先保护错误、动态及 HTML 响应，再对清单内的成功版本化资源放行长缓存。不能只根据 `/assets/` 目录判定。

| 资源路径 / 匹配条件 | 源站响应头建议 | EdgeOne 节点缓存 | 浏览器缓存 | 发布更新方式 |
| --- | --- | --- | --- | --- |
| `/`、`.html`、无扩展名页面（如 `/projects/pack-pilot`、`/lab/postcard`）、带斜杠入口、合适的页面导航回退 | `Cache-Control: no-cache`，正确 `Content-Type: text/html`，有能力时提供 ETag/Last-Modified | 本轮明确不缓存；包含无扩展名 URL，不能仅匹配 `.html` | 每次使用前重新验证，可验证后复用 | 资源就绪后再更新 HTML；清理此前可能存在的 HTML 节点缓存 |
| Vite manifest 中实际生成、内容变化即换 URL 的 JS/CSS/图片/字体，且为成功响应 | `Cache-Control: public, max-age=31536000, immutable`；正确 MIME | 长缓存 / 遵循源站（以控制台能力和检查结果为准） | 一年 immutable | 新 URL；保留旧分块过渡，不覆盖同名内容 |
| 非哈希 `/brand-mark.webp`、`/favicon.ico`、其他 favicon、`/og-card.webp`、以后可能存在的 manifest | 本轮 `Cache-Control: no-cache`，以后按变更频率选择短 TTL 或版本化 | 本轮不缓存，若启用短 TTL 需记录刷新步骤 | 验证后复用 | 替换后刷新相应准确 URL；不要把所有 public 资源缓存一年 |
| `/rss.xml`、`/sitemap.xml`、`/robots.txt`、未来非哈希搜索索引/公开内容 JSON | `Cache-Control: no-cache`；RSS/XML/JSON MIME 正确 | 本轮不缓存 | 验证后复用 | 与文章、路由元数据同次发布并刷新改变的 URL |
| 缺失 JS/CSS/图片/JSON/XML/字体、404、5xx | `Cache-Control: no-store`，真实错误状态，不返回 200 首页 | 明确不缓存错误；检查状态码缓存 TTL 和旧错误缓存 | 不缓存 | 补全资源后清理曾缓存的错误 URL，重新验证 |
| 如将来接入 API、登录态、用户专属内容 | 按内容使用 `private, no-store` 等；本次不存在此类自建响应 | 绕过共享缓存 | 按业务决定，不复用用户专属响应 | 独立审查，不能套静态规则 |

`no-cache` 表示重新验证，不等于禁止保存。节点“不缓存”和浏览器“重新验证”是两层独立设置，不能用全局强制缓存覆盖。规则优先级、匹配能力及实际响应需要在目标 EdgeOne 站点复核。[节点缓存 TTL](https://edgeone.ai/document/46175)、[浏览器缓存 TTL](https://edgeone.ai/document/46176)。

## 3. 深层路由与真实错误

1. 优先匹配实际文件及生成的 `path.html` / `path/index.html`，保留其独立 canonical、Open Graph、文章元数据。
2. 无扩展名页面导航可由明确的路由策略回退；当前实际页面均有构建入口。未知页面应返回带 404 状态的 `404.html`，客户端再显示未找到页面。
3. 缺失资源、API、字体、JSON、XML 不得进入 200 首页回退。不能为了 History 路由把所有路径都改写成首页。
4. 根目录 `edgeone.json` 是此前保留的 **Pages 产品专属配置**。普通源站 + EdgeOne CDN 不会自动读取它；`_headers` 同样不是通用标准。本轮未修改真实 CDN 配置，也未把任何猜测的服务器配置当作可直接部署的成品。
5. 若最终采用 Pages，应再检查其规则对缺失资源的实际响应及状态码；官方示例优先匹配已有资源，但不等于已经满足本项目的缺失资源验收。[Pages 配置文档](https://pages.edgeone.ai/document/edgeone-json)。

## 4. 缓存键、查询参数与本地偏好

- 不全局忽略 query。内容版本、图片宽高/格式处理、分页等确实改变响应的参数必须保留。只有证明不改变内容的参数才可按路径另行讨论。
- `/articles?q=…`、标签和锚点继续保持原有地址栏行为；缓存键调整不应删除它们。
- 收藏、主题、阅读偏好留在浏览器，不注入共享的个性化 HTML。当前无用户专属 SSR 响应。
- 当前没有独立图片域名；如未来启用根据 Origin 变化的响应，需要核对 Vary 支持与对应缓存分离，而非机械加一个响应头。[缓存键官方说明](https://edgeone.ai/document/56196)。

## 5. 发布顺序（未执行）

1. 本地验证构建、稳定 URL、静态 Meta、资源清单与 PNG 导出；源站部署根路径与 `meta.siteUrl` 使用实际值。
2. 先上传新版本哈希资源并抽样检查状态、MIME、压缩和完整性。
3. 再发布对应 HTML、路由入口、RSS、Sitemap 和非版本化元数据。
4. 刷新发生变化的非版本化 URL；不得靠所有请求追加 `Date.now()`。
5. 保留旧版本分块一段明确的过渡期；根据最长打开会话和发布频率确定时长，不立即清空旧 `/assets/`。
6. 对旧标签页故意触发延迟加载，检查恢复提示和编辑页保留行为。不自动刷新，不清空用户存储。[Vite 分块加载失败说明](https://vite.dev/guide/build#load-error-handling)。

## 6. 明信片与导出

当前 Canvas 使用代码绘制和本地 PNG/JPEG/WebP 解码，没有从第三方图片 URL 抓取素材。网站同一域名接入 CDN 不会自动变成跨域。头像和封面保持原色，未向导出画布引入头像。

若后续增加独立图片域名：设置图片 `src` **之前**设置合适的 `crossOrigin`，资源端返回匹配的 CORS 响应，避免源站与 EdgeOne 重复/冲突头；Origin 可变时依据产品的 Vary 能力配置缓存分离。只放行需要导出的可控素材，不给全站接口加 `Access-Control-Allow-Origin: *`，不关闭浏览器安全检查。

本地需验证素材可显示、Canvas 可画、PNG 可解码且有真实像素；**EdgeOne 命中/未命中两种情况下的素材与导出行为仍待上线后验证**。

## 7. 上线后验证清单（全部待执行）

- [ ] 确认真实源站产品、静态文件优先级、规则生效位置及 CDN 域名 / 回源 Host / TLS。
- [ ] 首页、文章、标签、6 个作品详情、工坊子页、查询串与锚点直接访问/刷新，原始 HTML Meta 正确。
- [ ] HTML 与无扩展名路径节点不缓存，浏览器收到 no-cache；连续请求检查实际状态和响应头。
- [ ] Vite 清单里的成功哈希资源可长缓存；旧分块过渡期仍可访问。
- [ ] 非哈希 public 文件、RSS/Sitemap 不误用一年缓存，改变后刷新准确 URL。
- [ ] 缺失 JS/CSS/图片/XML/JSON/字体/API 返回正确错误状态和 MIME，不是 200 首页；错误不长期缓存。
- [ ] 按实际产品响应头或 EdgeOne 访问日志确认命中/未命中，不能仅凭请求更快来认定命中。
- [ ] Content-Type、Brotli/gzip、缓存键里的业务 query、跨源 Vary（如启用）分别检查。
- [ ] CDN 冷/热缓存时各导出一次 PNG，解码检查尺寸、文字、本地图片和贴纸实际像素。
- [ ] 生产构建无 localhost、本机盘符、开发服务引用；公开 URL、canonical、部署基路径一致。
- [ ] 更新时的旧页面分块失败有明确恢复入口；未导出编辑内容保留；无自动刷新循环。
- [ ] 如以后加入音频，单独核验 206、Content-Range、Accept-Ranges 与拖动播放。本阶段没有该音频功能，不声称已验证。
